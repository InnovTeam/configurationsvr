server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/InnovTeam/global-config/
          search-paths:
          - authentication
  application:
    name: globalConfiguration